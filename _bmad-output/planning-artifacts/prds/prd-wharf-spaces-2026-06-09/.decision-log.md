# Decision Log — Wharf Spaces Rebuild PRD

_Workspace: `_bmad-output/planning-artifacts/prds/prd-wharf-spaces-2026-06-09/`_
_Recreated from handoff.md on 2026-06-11 (original log file was not persisted)_

---

## Session 1 — 2026-06-09 / 2026-06-10

### DL-001 — Single app binary
**Decision:** One app on the App Store; tenant branding and config loaded at runtime. No per-tenant builds.
**Rationale:** Simpler release process; AND Digital maintains one binary.

### DL-002 — Platform operator model
**Decision:** AND Digital is the platform operator — approves new tenants and controls email domain allowlists via the onboarding form web app, not Firebase console.
**Rationale:** Self-serve philosophy; minimise AND Digital's ongoing manual maintenance burden.

### DL-003 — V1 deadline
**Decision:** End of August 2026 (2026-08-31).
**Team:** 5 developers + 2 product people, mixed disciplines.

### DL-004 — User roles (v1)
**Decision:** Four roles — End User (mobile), Company Admin (mobile), AND Digital / Platform Operator (web), Building Admin (web only).
**Note:** Building Admin mobile access is explicitly out of scope for v1.

### DL-005 — Auth strategy
**Decision:** Per-tenant config supporting Google, Microsoft, Apple (iOS), and email/password. AND Digital controls which email domains are allowed per tenant (not the Company Admin). Replaces current hardcoded @and.digital Google-only config.

### DL-006 — Scope boundary
**Decision:** In-scope v1: mobile app (iOS + Android), React web app (two screens), multi-tenancy, feature parity, GDPR/security/observability.
**Out of scope v1:** User-facing web booking portal, billing, floor maps, recurring bookings, calendar integration, hardware integration, dedicated company admin web interface.

### DL-007 — Design approach
**Decision:** New mobile app matches existing screens as closely as possible. Screenshots in /docs/screenshots/. Some new screens need design (login, events modal, guest booking, waitlist state, etc.). Tenant-specific strings become configurable; layouts and interaction patterns carry over.

### DL-008 — NFR concerns to address
**Decision:** Five NFR sections required in the PRD: GDPR / privacy / data governance, Security, Observability / logging, Support model (not yet decided — needs discussion), Performance / reliability.

### DL-009 — Working mode
**Decision:** Coaching path, Vision + Features entry point.
**Feature group order agreed (14 groups):**
Mobile: (1) Auth & onboarding, (2) Desk booking, (3) Parking booking, (4) Events & notes, (5) Who's in list, (6) Push notifications, (7) Company admin mobile capabilities.
Web: (8) Tenant onboarding form, (9) Parking occupancy view.
Platform/cross-cutting: (10) Multi-tenancy & white-label theming, (11) GDPR/privacy, (12) Security, (13) Observability & logging, (14) Support model.

---

_Ongoing decisions added below as the coaching session continues._

## Session 2 — 2026-06-11

### DL-010 — Auth access model
**Decision:** Individual allowlist model (not domain-level). Company Admin registers each user by email address. Anyone not on the allowlist sees "please contact your company administrator" and cannot proceed.

### DL-011 — Demo/reviewer login purpose
**Decision:** Demo login mode exists to satisfy App Store review requirements (Apple reviewers need a way in without a real tenant account). Not a general demo product feature.

### DL-012 — First sign-in experience
**Decision:** No onboarding steps after sign-in — user lands directly on the booking screen. Push notification permission prompt shown on first sign-in.

### DL-013 — Email/password verification method
**Status:** Deferred. OTP or email link — to be confirmed before architecture. Logged as OQ-001.

### DL-014 — Desk booking: feature parity confirmed
**Decision:** Desk booking in the rebuild matches the current app (week calendar, AM/PM/All Day slots, per-tenant capacity, waitlist, guest booking, cancellation). No new behaviours added to v1.

### DL-015 — Tenant-configurable strings on booking screen
**Decision:** "Events in the clubhouse" → tenant-configurable string (generic default: "Events in the office"). "ANDis" count label in Who's In → also tenant-configurable. Full treatment in feature groups 4 (Events & Notes) and 10 (Multi-tenancy).

### DL-016 — Parking booking window rules: tenant-configurable
**Decision:** The booking window rules (how far ahead, time-of-day cutoffs, capacity pooling) are tenant-configurable, not AND Digital-specific hardcoded logic. Platform enforces rules using server time (anti-manipulation).

### DL-017 — Parking capacity model: per-tenant groups
**Decision:** Each tenant defines their own internal groups with their own parking allocations. Replaces the hardcoded BusinessUnit enum (murray/tenzing/adams/unknown).

### DL-018 — Events/notes: any user can author
**Decision:** Any signed-in user (not just admins) can create/edit the shared daily note for their tenant. Simplifies role-based access for this feature.

### DL-019 — isOfficeClosed deferred to v2
**Decision:** The `isOfficeClosed` field (formerly `isClubhouseClosed`) is not surfaced in the v1 UI. Data model preserves the field for a future version.

### DL-020 — Events banner label: tenant-configurable
**Decision:** "Events in the clubhouse" → tenant-configurable string. Default: "Events in the office".

### DL-021 — Who's In: fixed section headers
**Decision:** "Who's in?" and "Who's parking?" remain as fixed strings — not tenant-configurable. Member count term ("ANDis") is tenant-configurable.

### DL-022 — Company Admin mobile capabilities: guest parking only
**Decision:** Company Admins have no dedicated mobile admin UI in v1. Their only elevated mobile permission is guest parking (FR-019). All tenant configuration is done via the web platform.

### DL-023 — Allowlist management is on the web platform
**Decision:** Company Admins add users to the allowlist via the web platform, not the mobile app. Revises earlier assumption in DL-010 which left the surface unspecified.

### DL-024 — Tenant onboarding form captures: domain, auth provider, first admin
**Decision:** The AND Digital onboarding form provisions: domain name, auth provider config, first Company Admin email. Further configuration (capacity, groups, branding) done by Company Admin post-onboarding.

### DL-025 — Allowlist management on web platform (Company Admin)
**Decision:** Company Admins add/remove individual user emails from their tenant's allowlist via the web platform.

### DL-026 — Tenant suspension: lock out immediately, data preserved, reversible
**Decision:** Suspending a tenant immediately locks out all users. Data persists. Reinstatement restores full access without data loss.

### DL-027 — Permanent tenant removal: capability exists, usage discretionary
**Decision:** Hard delete functionality built in v1. Whether AND Digital uses it immediately is their call. Data retention/GDPR obligations on deletion logged as OQ-002.

### DL-028 — Building Admin parking occupancy view: out of scope v1
**Decision:** Feature group 9 (Building Admin parking occupancy view) removed from v1 scope. Scope section and web app description updated accordingly.

### DL-029 — Branding configured by Company Admin, not AND Digital
**Decision:** Tenant branding (colours, logo) configured by Company Admin via the web platform. AND Digital does not set branding during onboarding.

### DL-030 — App name: TBD
**Decision:** App name will change from the current AND Digital-specific name. New name not yet decided. Logged as OQ-003.

### DL-031 — User removal triggers data deletion
**Decision:** Removing a user from the allowlist must delete their personal data and booking history. Not required to be instant but must be part of the removal process.

### DL-032 — Right to erasure: manual handling in v1
**Decision:** No in-app erasure flow in v1. Requests handled manually by AND Digital or Company Admin out of band.

### DL-033 — Tenant isolation is a server-side security requirement
**Decision:** Tenant isolation must be enforced at the API/backend layer, not just the data model or UI. A user in Tenant A cannot access Tenant B's data regardless of client behaviour.

### DL-034 — Observability: tenant-attributed, user consent toggle retained
**Decision:** All crash and analytics events attributed to tenant. Analytics/Crashlytics consent toggle carries forward from current app (login screen).

### DL-035 — Support model v1: direct contact to AND Digital
**Decision:** Company Admins contact AND Digital directly for support. No in-app or ticketing mechanism in v1. Formal support model deferred to a future phase.

---

## Session 3 — 2026-06-12 (Finalize)

### DL-036 — Building Admin role: retained in Users table, annotated out of scope
**Decision:** Building Admin row kept in the Vision > Users table with a note that no Building Admin capability is delivered in v1. Not removed, to signal v2 intent.
**Audit finding:** DL-004 and DL-028 were in conflict — role defined but feature removed from scope.

### DL-037 — Performance/Reliability NFR added (NFR-5)
**Decision:** NFR-5 written using user-perceived requirements only — no specific latency targets. Five areas covered: booking responsiveness, real-time list currency, concurrent booking correctness, offline behaviour, platform availability.
**Rationale:** Specific second-level targets were not grounded in product data; architect to determine concrete thresholds in technical spec.

### DL-038 — Who's In fixed headers made explicit
**Decision:** FR-024 updated to explicitly state that "Who's in?" and "Who's parking?" are fixed strings and not tenant-configurable.
**Audit finding:** DL-021 decision was present in the log but the PRD text only implied fixed headers; the distinction vs. the configurable member term (FR-026) was not stated.

### DL-039 — Duplicate OQ-001 removed
**Decision:** Duplicate OQ-001 entry removed from Open Questions section. Three open questions remain: OQ-001, OQ-002, OQ-003.

### DL-040 — Cross-group parking reallocation: nightly at 9pm (FR-040)
**Decision:** At 9pm server time, unbooked parking spaces across all groups within a tenant are pooled and allocated to waitlisted users from any group, in waitlist order. Added as FR-040.
**Rationale:** Groups have different capacity allocations; groups with smaller allocations frequently have waitlist entries while larger-allocation groups have unused spaces. 9pm was chosen as it aligns with the end of most users' working day.
**Note:** This is a fixed platform behaviour, not a tenant-configurable rule. FR-018 updated to remove misleading "tenant's configured rules for when capacities pool" language.

### DL-047 — Web platform: basic functional tool, low design priority
**Decision:** The web platform (FR-031–FR-034) does not require high-fidelity UX design. It should be clean and functional, following the mobile app's visual style where convenient. Engineering effort should be proportionate to its low frequency of use.

### DL-046 — Clean start: no data migration from existing app
**Decision:** The rebuild does not migrate existing Firestore data. AND Digital will re-enter tenant config and users from scratch on the new platform. No data transformation or migration tooling is required.

### DL-045 — Parking booking window: one configurable parameter + fixed 12pm unlock (FR-017)
**Decision:** The only tenant-configurable parameter is the number of days ahead bookings can be made. The 12pm unlock rule is fixed: before 12pm, window is N−1 days; at/after 12pm, window is N days. The 12pm time is not configurable.
**Configured by:** Company Admin (not AND Digital).

### DL-044 — One auth provider per tenant; domain-based discovery (FR-001, FR-031)
**Decision:** Each tenant has exactly one authentication provider (not "one or more" as previously stated). Sign-in flow: user enters email → app resolves tenant from domain → presents that tenant's provider.
**Provisioning form fields confirmed:** Tenant Name, Email Domain, Provider (Google / Microsoft / Apple / email/password), First Company Admin email.

### DL-043 — Apple Hide My Email: known unsupported constraint in v1 (FR-001)
**Decision:** Apple Sign-In users who enable Hide My Email are unsupported in v1. Their relay address (@privaterelay.appleid.com) cannot be matched to a tenant domain. Accepted as a known constraint; not a blocker for v1.

### DL-042 — Waitlist slot-matching rules (FR-012)
**Decision:** Slot-type matching on cancellation: AM cancel → AM waitlist only; PM cancel → PM waitlist only; All Day cancel → All Day, AM, and PM waitlist entries all eligible (equal priority, next in queue regardless of type). An All Day cancellation can be split across AM + PM if both are next in the queue.
**Decision:** Booking promotion stands regardless of push notification delivery success.

### DL-041 — User auto-creation on sign-in: intentionally replaced by allowlist model
**Decision:** The existing app auto-creates user accounts on first sign-in via a POST /v1/user call requiring a Google-Access-Token header. This pattern is intentionally not carried forward in the rebuild. The rebuild uses the allowlist model (FR-002): users must be added by their Company Admin before they can sign in. A user not on the allowlist sees an error directing them to contact their administrator.
**Reconciliation note:** The subagent found the auto-creation pattern in project-context.md. Ashley confirmed FR-002 correctly describes the new intended behaviour.

### DL-048 — Email/password removed from v1 auth provider options
**Decision:** Email/password is not a supported authentication provider in v1. Supported providers are Google, Microsoft, and Apple only. Email/password deferred to v2.
**Rationale:** Adds significant build complexity (verification flow, OTP/email-link mechanism, password reset) with no confirmed v1 tenant who requires it. All expected v1 tenants operate with a corporate identity provider (Google or Microsoft). Apple SSO is available for tenants without a corporate IdP.
**Consequence:** FR-003 (email/password verification) removed. FR-004 renumbered to FR-003. FR-031 provider list updated. OQ-001 closed — moot.
**Consequence:** FR-003 (now App Store reviewer access) updated: demo bypass uses a real preconfigured tenant+user account rather than a platform-level email/password credential. Architecture to determine the appropriate SSO provider for the demo tenant.

### DL-049 — GDPR: immediate hard delete acceptable on permanent tenant removal
**Decision:** No GDPR-mandated retention period applies before hard-deleting a permanently removed tenant's data. FR-034 updated to reflect immediate hard delete as the v1 behaviour.
**Rationale:** GDPR Article 17 favours deletion, not retention. Booking and user profile data carries no statutory retention obligation (not financial records, not transaction logs). Post-deletion SARs and erasure requests are satisfied by the absence of data — a defensible position.
**Note:** Tenant contracts should address data handling on termination. This is a commercial/legal matter for AND Digital to handle outside the product; not a product requirement.
**OQ-002 closed.**

### DL-050 — App name: ParkANDPerch (working name, pending marketing sign-off)
**Decision:** App name is ParkANDPerch. Working name — final confirmation pending from the marketing team.
**Constraint:** No public-facing copy (App Store listing, onboarding screens, marketing materials) to be published until marketing sign-off is received.
**OQ-003 closed.**

### DL-051 — User-to-group assignment required on allowlist add; minimum one group per tenant
**Decision:** When a Company Admin adds a user to the allowlist (FR-032), they must also assign that user to a group. A user cannot be added without a group assignment. Each tenant must have at least one group configured (name + parking capacity) before any users can be added.
**Rationale:** FR-018 derives the parking capacity and waitlist view from the user's group. Without a group assignment, the booking logic has no basis to determine a user's capacity allocation. The minimum-one-group constraint ensures this precondition is always met before a user can sign in.
**Gap identified:** FR-032 previously only described allowlist management (access grant/revoke); group assignment was implied by FR-018 but never stated as a UI action.

### DL-052 — Auto-create Default group on tenant provisioning
**Decision:** When AND Digital provisions a new tenant (FR-031), the system automatically creates an initial group named "Default" with 0 parking capacity. The Company Admin can rename it, set capacity, or delete it (provided at least one other group exists and no users are assigned to it).
**Rationale:** Without a pre-existing group, Company Admins would be unable to add users (FR-032 requires group assignment) and would need to contact AND Digital for setup instructions. Auto-creating Default removes this friction and ensures every tenant is in a valid state immediately after provisioning.
**Additional constraint added to FR-038:** A group with assigned users cannot be deleted until those users are reassigned to another group.

### DL-053 — Google OAuth elevated scopes dropped in rebuild
**Decision:** The `contacts.readonly` and `user.organization.read` Google OAuth scopes used in the existing app are not requested in the rebuild.
**Rationale:** The existing app used these scopes to look up contacts and org membership during sign-in. The rebuild's allowlist model (FR-002) replaces this mechanism entirely — access is controlled by the Company Admin's allowlist, not by Google org data. Requesting elevated scopes without a functional need would create unnecessary friction at App Store and Play Store review.
**A-001 updated** in addendum to record this decision for the architecture team.

---

## Session 4 — 2026-06-15 (Finalize continued)

### DL-054 — PRD finalised
**Decision:** `prd.md` status set to `final`. All open questions closed (OQ-001 through OQ-003 resolved; OQ-004 pre-empted by DL-053). All critical and high rubric findings resolved across sessions 3–4. Medium and low findings addressed in polish pass.
**Changes in this session:** DL-048 (email/password removed), DL-049 (GDPR hard delete), DL-050 (ParkANDPerch name), DL-051 (user group assignment), DL-052 (Default group auto-creation), DL-053 (Google OAuth scopes dropped).
